# No hace falta que Vercel suba ni considere esto para el sitio estatico
# (RioSinu.html, Mapa.html, Index.html, casa.html, etc.)

# Backend viejo de la casa vibratoria (Flask + MySQL): vivia en Render,
# ya no se usa. OJO: "requirements.txt" NO va aqui -- sin la barra "/"
# adelante, un patron de .vercelignore excluye ese nombre en CUALQUIER
# carpeta (asi se rompio antes "api/requirements.txt", el que si hace
# falta para la API real en Vercel, api/index.py + Supabase).
/api.py
api_sensores_LAB.py
Procfile
servidor_mapa_gdb.py
INICIAR_MAPA_GDB.bat
requirements_gdb.txt

# Datos crudos originales del rio Sinu (744MB) - RioSinu.html usa las
# versiones livianas de rio_sinu_data/, esto no hace falta desplegarlo
Sinu (2)/

# GeoTIFF reales (DEM + Landsat): Vercel (plan gratis) tambien bloquea
# archivos de mas de 100MB, igual que GitHub -- asi que esto NO se puede
# subir por ahora tal cual. Se queda fuera para no tumbar el deploy
# completo. Los checkboxes de "tif real" en RioSinu.html/Mapa.html van
# a seguir fallando en el sitio en vivo hasta que se resuelva esto
# (bajar resolucion para quedar bajo 100MB, o usar otro storage).
capas_pesadas/

# Proyectos aparte que no usa ningun html del sitio
# (ojo: agente1/ SI se usa -- Index.html carga agente1/agente.js y
# agente1/agente-panel.css para el widget "Asistente ICDE", no tocar)
NubePuntos/
Webscraping/
DB Geo andes/

# Basura de entorno/editor
.vs/
__pycache__/
*.pyc

# Copias de la base de datos (eran solo para migrar una vez)
*.sql
import_ensayos.bat
